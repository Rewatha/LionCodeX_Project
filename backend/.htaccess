# .htaccess - Security and Configuration for Backend
# File: backend/.htaccess

# Enable rewrite engine
RewriteEngine On

# Prevent directory listing
Options -Indexes

# Protect sensitive files
<FilesMatch "^(config\.php|\.htaccess)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# Set default charset
AddDefaultCharset UTF-8

# Enable CORS for API requests (adjust as needed)
Header set Access-Control-Allow-Origin "*"
Header set Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS"
Header set Access-Control-Allow-Headers "Content-Type, Authorization"

# PHP Security Settings
php_flag display_errors Off
php_flag log_errors On
php_value error_log ../logs/php_errors.log

# File upload limits
php_value upload_max_filesize 5M
php_value post_max_size 6M
php_value max_execution_time 300
php_value max_input_time 300

# Session security
php_value session.cookie_httponly 1
php_value session.cookie_secure 0
php_value session.use_only_cookies 1

# Prevent access to backup files
<FilesMatch "\.(bak|config|sql|fla|psd|ini|log|sh|inc|swp|dist)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# Block bad bots and scanners
RewriteCond %{HTTP_USER_AGENT} ^.*(bot|spider|crawl|scanner).*$ [NC]
RewriteRule .* - [F,L]

# Force HTTPS (uncomment when on production with SSL)
# RewriteCond %{HTTPS} off
# RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Custom error pages (optional)
ErrorDocument 403 /error/403.html
ErrorDocument 404 /error/404.html
ErrorDocument 500 /error/500.html
```

**Save the file!** ✅

---

## **🎉 ALL BACKEND FILES COMPLETED!**

You now have a **complete, working backend**!

---

## **📋 FINAL FILE STRUCTURE CHECK:**
```
C:\xampp\htdocs\LionCodeX_Project\
├── index.html
├── pages/
│   ├── services.html
│   ├── about.html
│   ├── contact.html
│   ├── auth.html
│   ├── user-dashboard.html
│   ├── admin-dashboard.html
│   └── staff-dashboard.html
├── css/
├── js/
├── images/
└── backend/                          ✅ COMPLETE!
    ├── config.php                    ✅
    ├── auth.php                      ✅
    ├── dashboard-api.php             ✅
    ├── admin-api.php                 ✅
    ├── staff-api.php                 ✅
    ├── submit_form.php               ✅
    ├── .htaccess                     ✅
    └── classes/
        ├── Database.php              ✅
        ├── User.php                  ✅
        └── Response.php              ✅